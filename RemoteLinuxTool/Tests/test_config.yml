user_config:
  user: 'user1'
target_config:
  target: "myTarget"
ext_tool_config:
  scp_path: 'myPath/scp.exe'
  ssh_path: 'myPath/ssh.exe'
script_config:
  scripts_dir: 'scripts'
  scripts:
    - 'GetResourceInfo.sh'
log_config:
  target_logs_to_save:
    - '/var/log/resourceInfoLog.txt'
    - '/var/log/messages'
  destination_log_dir: 'logs'